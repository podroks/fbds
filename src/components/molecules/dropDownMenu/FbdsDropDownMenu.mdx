import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsDropDownMenuMeta, { Default, WithCheckbox, WithTheme } from './FbdsDropDownMenu.stories';
 
<Meta of={FbdsDropDownMenuMeta} />
 
# DropDownMenu

<Canvas of={Default} />

<Controls of={Default} />

<Canvas of={WithCheckbox} />

<Canvas of={WithTheme} />
