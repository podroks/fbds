import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsInputRangeMeta, { Default } from './FbdsInputRange.stories';
 
<Meta of={FbdsInputRangeMeta} />
 
# InputRange

<Canvas of={Default} />

<Controls />
