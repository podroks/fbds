import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsInputMeta, { Default } from './FbdsInput.stories';
 
<Meta of={FbdsInputMeta} />
 
# Input

Le composant `FbdsInput` agit comme un conteneur pour l'ensemble des composants de saisie.

<Canvas of={Default} />

<Controls />
