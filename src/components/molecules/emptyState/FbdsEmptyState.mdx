import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsEmptyStateMeta, { Default } from './FbdsEmptyState.stories';
 
<Meta of={FbdsEmptyStateMeta} />
 
# EmptyState

<Canvas of={Default} />

<Controls />
