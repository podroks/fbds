import { Canvas, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsColorMeta, { Color } from './FbdsColor.stories';
 
<Meta of={FbdsColorMeta} />
 
# Couleurs

Les couleurs par défaut utilisées par **FBDS** sont remplacables via une règle CSS personnalisée :

```css
:root {
  --fbds-base-surface: <votre-couleur>;
}
```

<Canvas of={Color} sourceState="none" withToolbar={false} />
