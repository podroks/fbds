import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsSwitchMeta, { Default } from './FbdsSwitch.stories';
 
<Meta of={FbdsSwitchMeta} />
 
# Switch

<Canvas of={Default} />

<Controls />
