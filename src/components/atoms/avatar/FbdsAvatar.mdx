import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsAvatarMeta, { Default } from './FbdsAvatar.stories';
 
<Meta of={FbdsAvatarMeta} />
 
# Avatar

<Canvas of={Default} />

<Controls />
