import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsSegmentedMeta, { Default } from './FbdsSegmented.stories';
 
<Meta of={FbdsSegmentedMeta} />
 
# Segmented

<Canvas of={Default} />

<Controls />
