import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsSpinnerMeta, { Default } from './FbdsSpinner.stories';
 
<Meta of={FbdsSpinnerMeta} />
 
# Spinner

<Canvas of={Default} />

<Controls />
