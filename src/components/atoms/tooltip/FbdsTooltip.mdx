import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsTooltipMeta, { Default } from './FbdsTooltip.stories';
 
<Meta of={FbdsTooltipMeta} />
 
# Tooltip

<Canvas of={Default} />

<Controls />
