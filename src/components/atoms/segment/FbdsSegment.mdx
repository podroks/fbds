import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsSegmentMeta, { Default } from './FbdsSegment.stories';
 
<Meta of={FbdsSegmentMeta} />
 
# Segment

<Canvas of={Default} />

<Controls of={Default} />
