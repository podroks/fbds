import { Canvas, Controls, Meta } from '@storybook/addon-docs/blocks';
 
import FbdsTabsMeta, { Default } from './FbdsTabs.stories';
 
<Meta of={FbdsTabsMeta} />
 
# Tabs

<Canvas of={Default} />

<Controls />
